let firstBtn = document.querySelector("st-btn");
let secondBtn = document.querySelector("nd-btn");

function addShade(element) {
  element.classList.add("shade");
}

function removeShade(element) {
  element.classList.remove("shade");
}
